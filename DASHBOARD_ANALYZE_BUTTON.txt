=== ADD THIS TO YOUR DASHBOARD.PHP ===

After the "Space Details" button, add this "Analyze Space" button:

<a href="#" class="btn-analyze-space" onclick="analyzeSpace(event)">
    <i class="fas fa-chart-line"></i>
    Analyze Vastu
</a>

=== ADD THIS CSS ===

.btn-analyze-space {
    background: linear-gradient(135deg, #9B59B6, #8E44AD);
    color: white;
    border: none;
    padding: 1.2rem 3rem;
    border-radius: 50px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    box-shadow: 0 4px 15px rgba(155, 89, 182, 0.3);
    margin-bottom: 1.5rem;
    text-decoration: none;
}

.btn-analyze-space:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(155, 89, 182, 0.4);
}

=== ADD THIS JAVASCRIPT ===

async function analyzeSpace(event) {
    event.preventDefault();
    
    // Get user's latest space
    try {
        const response = await fetch('api_get_user_data.php');
        const result = await response.json();
        
        if (result.success && result.spaces && result.spaces.length > 0) {
            const latestSpace = result.spaces[0];
            window.location.href = `vastu_results.html?space_id=${latestSpace.id}`;
        } else {
            alert('Please create a space first before analyzing!');
            window.location.href = 'space.php';
        }
    } catch (error) {
        console.error('Error:', error);
        alert('Failed to load spaces. Please try again.');
    }
}

